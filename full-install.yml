---
- name: Full install of ACM, import clusters, clustersets, submariner-addon, multi-cockroachdb deployment
  hosts: localhost
  gather_facts: false

  roles:
    - install-acm
    - import-clusters
    - create-clusterset
    - install-submar-addon
    - install-multi-cockroachdb