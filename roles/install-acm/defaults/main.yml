# Switch to import or delete clusters
# values present or absent
# setState: absent

# yml file for OpenShift Container Platform pull secret retreive from --> cloud.redhat.com/openshift/install/pull-secret
# place yml file in roles/acm/files
ocp_pull_secret: acm-test01-secret.yml

ocp_pull_secret_name: acm-test01-pull-secret